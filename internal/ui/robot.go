package ui

// Robot is SetFree's tiny mascot. It's plain ASCII so it renders correctly
// in every terminal without any locale/Unicode detection, and it's short
// enough not to feel like a banner. Shown only on the no-args landing
// screen and sparingly during interactive setup — never on a normal launch.
const Robot = ` ,-.
(o.o)
|>-<|
/   \`
